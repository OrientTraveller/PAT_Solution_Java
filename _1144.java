package PATFoo;

public class _1144 {
/*
 * #include <stdio.h>
#include <stdlib.h>
#define BOOL int
#define TRUE 1
#define FALSE 0
#define max 100010

int main()
{
    BOOL flag[max] = {FALSE};
    int round;
    scanf("%d",&round);
    for(int i = 0 ; i < round ; i++){
        int temp;
        scanf("%d",&temp);
        if(temp < max && temp > 0)
        flag[temp] = TRUE;
    }
    for(int i = 1 ; i < max ; i++){
        if(flag[i] == FALSE){
            printf("%d\n",i);
            break;
        }
    }
    return 0;
}
 */
}
